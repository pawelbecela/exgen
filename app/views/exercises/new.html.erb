<%= form_with url: exercises_path do |form| %>
  <% @notes.each do |note| %>
    <%= check_box_tag "notes[]", note %>
    <%= note %>
  <% end %>

  <%= form.label :name %>
  <%= form.text_field :name %>
  <%= form.submit "gen" %>
<% end %>